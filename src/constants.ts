// Token thresholds for summarization
export const SUMMARIZATION_TOKEN_THRESHOLD = 80_000;
export const RECENT_TURNS_TO_KEEP = 10;

// File size limits
export const MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024;

// Bash defaults
export const DEFAULT_BASH_TIMEOUT_MS = 30_000;

// Diff settings
export const DIFF_CONTEXT_LINES = 3;

// Web fetch
export const FETCH_TIMEOUT_MS = 30_000;
